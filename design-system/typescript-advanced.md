# 🔷 TypeScript 심화

> 관련 컴포넌트: Button

---

## Polymorphic 컴포넌트

- Polymorphic 컴포넌트가 무엇인지, 왜 설계했는지 설명해주세요.
- `as` prop 타입을 어떻게 구현하셨나요?
- `React.ElementType`이 뭔가요?
- `Omit`을 쓴 이유가 무엇인가요?
- `forwardRef`와 제네릭을 함께 쓸 때 왜 타입 단언(`as`)이 필요한가요?

---

## 유틸리티 타입

- `Omit`, `Pick`, `Partial`, `Required`의 차이를 설명해주세요.
- `React.ComponentPropsWithoutRef`와 `React.ComponentPropsWithRef`는 언제 각각 쓰나요?
